import { Text, View } from "react-native";
import moment from "moment-timezone";

const Dashboard = () => {
  // const serverTime = moment(new Date());

  return (
    <View>
      <Text>Current Time: hi</Text>
    </View>
  );
};

export default Dashboard;
